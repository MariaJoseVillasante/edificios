<% if @posts.errors.any? %>
    <div id="error_explanation">
        <h2>
            <%= pluralize(@posts.errors.count, "error") %>
                prohibited this post from being saved:
        </h2>
        <ul>
        <% @posts.errors.full_messages.each do |msg|  %>
            <li><%= msg %></li>
        </ul>
    </div>
<% else %>
    <h1>Tu post ha sido agregado con éxito</h1>
<% end %>